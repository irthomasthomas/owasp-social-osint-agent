[pytest]
# Tell pytest to look for tests only in the 'tests' directory.
# (Assumes you've moved the tests/ dir to the root as recommended)
testpaths = tests

# Standard naming convention for test files.
python_files = test_*.py

# Default command-line options to use every time you run pytest.
# -v: verbose mode (shows test names instead of just dots)
# --strict-markers: Fails the test run if you use an unregistered marker.
addopts = -v --strict-markers

# Register custom markers to categorize tests.
markers =
    api: tests that interact with (mocked) platform APIs
    slow: tests that are known to be slow

# Show log messages of INFO level and higher directly in the test output.
log_cli = true
log_cli_level = INFO